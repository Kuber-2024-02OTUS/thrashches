apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc
  namespace: homework
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi